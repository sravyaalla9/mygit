sum = SUM(Sales[SalesAmount])
average = AVERAGE(Sales[SalesAmount])
maximum = MAX(Sales[SalesAmount])
minimum = MIN(Sales[SalesAmount])
count = COUNT(Sales[SalesOrderNumber])
count rows = COUNTROWS(Sales)
count blank = COUNTBLANK(Sales[SalesOrderNumber])+0
distinctcount = DISTINCTCOUNT(Sales[SalesOrderNumber])
SUMX = SUMX(Sheet1,Sheet1[quantity]*Sheet1[unit price])
AVGX = AVERAGEX(Sheet1,Sheet1[quantity]*Sheet1[unit price])
MAXX = MAX(Sheet1[quantity])
MINX = MIN(Sheet1[quantity])
nested concatenate = CONCATENATE(Orders[Category],CONCATENATE("_",Orders[Sub-Category]))
another way to concatinate = Orders[Category] & "-" & Orders[Sub-Category]
concatenate = CONCATENATE(Orders[Category],Orders[Sub-Category])
Left = LEFT(Orders[Order ID],2)
Right = RIGHT(Orders[Order ID],6)
mid = MID(Orders[Order ID],4,4)
upper case = UPPER(Orders[Category])
lowercase = LOWER(Orders[Category])
length = LEN(Orders[lowercase])
trim = TRIM("    power bi     ")
substitute = SUBSTITUTE(Orders[Sub-Category],"P","Q")
substitute particular letter = SUBSTITUTE(Orders[Sub-Category],"P","Q",2)
format = FORMAT(Orders[Sales],"#,##0")
format date = FORMAT(Orders[Order Date],"YYYY")
format date 1 = FORMAT(Orders[Order Date],"Q")
format date 2 = FORMAT(Orders[Order Date],"DDDD")
format date 3 = FORMAT(Orders[Order Date],"DDD")
format date 4 = FORMAT(Orders[Order Date],"WW")
format date 5 = FORMAT(Orders[Order Date],"W")
calculated = CALCULATE(SUM(Orders[Sales]),Orders[Region]="central")
calculated 1 = CALCULATE(SUM(Orders[Sales]),Orders[Region] IN {"central","East"})
calculated using and = CALCULATE(SUM(Orders[Sales]),Orders[Region] IN {"central","East"} && Orders[Category]="Technology")
calculated using or = CALCULATE(SUM(Orders[Sales]),Orders[Region] IN {"central","East"} || Orders[Category]="Technology")



DATE AND TIME FUNCTIONS :
now = NOW()
today=TODAY()
utc now = UTCNOW()
utc today = UTCTODAY()
date add = DATEADD(Orders[Order Date],3,day)
date subtract = DATEADD(Orders[Order Date],-3,DAY)
date diff = DATEDIFF(Orders[Order Date],Orders[Ship Date],DAY)
year = YEAR(Orders[Order Date])
week number = WEEKNUM(Orders[Order Date])
week day = WEEKDAY(Orders[Order Date])
hour = HOUR(Orders[date subtract])
full time = TIME((HOUR(Orders[date subtract]),MINUTE(Orders[date subtract]),SECOND(Orders[date subtract]))
calender = CALENDAR(DATE(2023,01,01),DATE(2023,12,31))-in table not column,measure
calender min max = CALENDAR(MIN(Orders[Order Date]),MAX(Orders[Order Date]))
calender auto = CALENDARAUTO()
end of montg = ENDOFMONTH('calender auto'[Date])
end of quarter = ENDOFQUARTER('calender auto'[Date])
end of year = ENDOFYEAR('calender auto'[Date])
start of montg = STARTOFMONTH('calender auto'[Date])
start of year = STARTOFYEAR('calender auto'[Date])


IF:

IF = IF(Orders[Profit]>0,"profit","Loss")
Nested IF = IF(Orders[Profit]>0,"profit",IF(Orders[Profit]<0,"Loss","NPNL"))

nested if for days = IF(Orders[date diff]=0,"Same day",
IF(Orders[date diff]>=1 && Orders[date diff]<=3,"1-3 days",
IF(Orders[date diff]>3 && Orders[date diff]<=5,"4-5 days",
IF(Orders[date diff]>5 && Orders[date diff]<=7,"6-7 days","7+ days"))))

nested if for month = IF(Orders[month]=1,"January",
IF(Orders[month]=2,"Feb","others"))
calculate trable = CALCULATETABLE(Orders,Orders[Region]-"Central")
summerize = SUMMARIZE(Orders,Orders[Category],Orders[Sub-Category],"Total sales",SUM(Orders[Sales]))
summerize avg = SUMMARIZE(Orders,Orders[Category],Orders[Sub-Category],"Total sales",SUM(Orders[Sales]),"Avg sales",AVERAGE(Orders[Sales]))
ytd = TOTALYTD(SUM(Orders[Sales]),Orders[Order Date])
qtd = TOTALQTD(SUM(Orders[Sales]),Orders[Order Date])
mtd = TOTALMTD(SUM(Orders[Sales]),Orders[Order Date])
last yr ytd = CALCULATE([ytd],SAMEPERIODLASTYEAR(Orders[Order Date])) or  ytd-1 = CALCULATE([ytd],PARALLELPERIOD(Orders[Order Date],-1,YEAR))
last yr mtd = CALCULATE([mtd],SAMEPERIODLASTYEAR(Orders[Order Date])) or or mtd-1 = CALCULATE([mtd],PARALLELPERIOD(Orders[Order Date],-1,MONTH))
last year qtd = CALCULATE([qtd],SAMEPERIODLASTYEAR(Orders[Order Date])) or qtd-1 = CALCULATE([qtd],PARALLELPERIOD(Orders[Order Date],-1,QUARTER))
YTD-2 = CALCULATE([ytd],PARALLELPERIOD(Orders[Order Date],-2,YEAR)) )
MTD-2 = CALCULATE([mtd],PARALLELPERIOD(Orders[Order Date],-2,MONTH))
qtd-2 = CALCULATE([qtd],PARALLELPERIOD(Orders[Order Date],-2,QUARTER))
mtd+1 = CALCULATE([mtd],PARALLELPERIOD(Orders[Order Date],+1,MONTH))
ytd+1 = CALCULATE([ytd],PARALLELPERIOD(Orders[Order Date],+1,YEAR))
qtd+1 = CALCULATE([qtd],PARALLELPERIOD(Orders[Order Date],+1,QUARTER))
percentage ytd or year over year = ([ytd]-[last yr ytd])/[last yr ytd]
percentage mtd or month over month = ([ytd]-[last yr mtd])/[last yr mtd]
percentage qtd or quarter over quarter = ([qtd]-[last year qtd])/[last year qtd]
previos day = CALCULATE(SUM(Orders[Sales]),PREVIOUSDAY(Orders[Order Date]))
previos month = CALCULATE(SUM(Orders[Sales]),PREVIOUSMONTH(Orders[Order Date]))
previos year = CALCULATE(SUM(Orders[Sales]),PREVIOUSYEAR(Orders[Order Date]))
previous quarter = CALCULATE(SUM(Orders[Sales]),PREVIOUSQUARTER(Orders[Order Date]))
next year = CALCULATE(SUM(Orders[Sales]),NEXTYEAR(Orders[Order Date]))
next month = CALCULATE(SUM(Orders[Sales]),NEXTMONTH(Orders[Order Date]))
next day = CALCULATE(SUM(Orders[Sales]),NEXTDAY(Orders[Order Date]))
next quarter = CALCULATE(SUM(Orders[Sales]),NEXTQUARTER(Orders[Order Date]))
All = CALCULATE(SUM(Orders[Sales]),ALL(Orders[Category]))
All1 = CALCULATE(SUM(Orders[Sales]),ALL(orders))
allexcept = CALCULATE(SUM(Orders[Sales]),ALLEXCEPT(Orders,Orders[Region]))
All selected = CALCULATE(SUM(Orders[Sales]),ALLSELECTED(Orders[Category]))
selected value = "Total sales-" & SELECTEDVALUE(Orders[Region])
Is filtered = 
IF(ISFILTERED(Orders[Region]),"total sales-" & ALLSELECTED(Orders[Region]),"total sales")
